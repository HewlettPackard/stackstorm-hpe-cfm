version: 1.0

description: "Send plexxi switches to snow"

input:
  - switches

tasks:

    printswitches:
      with: <% ctx().switches %>
      action: servicenow.create_record table="u_cfm_asset" payload='<% item() %>'
