version: 1.0

description: "Perform fit on cfm controller"

input:
  - fabrics

tasks:

    fitfrabrics:
      with: <% ctx().fabrics %>
      action: hpecfm.perform_fit uuid='<% item(u_uuid) %>'
      
